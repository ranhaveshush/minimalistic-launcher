package com.ranhaveshush.launcher.minimalistic.ui.recyclerview

/**
 * An swiped item listener,
 * listens to the swiped item position.
 */
interface OnSwipedItemListener {
    fun onSwiped(position: Int)
}
